# Routes Config
#
# This file defines all application routes (Higher priority routes first)
#

module:testrunner
# module:jobs


GET     /                                       App.Index
GET     /App/Hello                              App.Hello
GET     /user                                   App.UserPage

# Auth
GET     /login                                  App.LoginPage
GET     /register                               App.RegisterPage
POST    /login                                  App.DoLogin
POST    /register                               App.DoRegister
POST    /logout                                 App.DoLogout

# Lists
GET     /lists                                  Lists.Index
GET     /lists/:name                            Lists.Show
POST    /lists/:name/insertOne                  Lists.InsertOne
POST    /lists/:name/insertMany                 Lists.InsertMany
POST    /lists/:name/clear                      Lists.Clear
GET     /lists/:name/backup                     Lists.Backup

# TreeDir
GET     /td/GetRoot                             TD.GetRoot
POST    /td/CreateDir/:parentID/:name           TD.CreateDir
POST    /td/CreateFile/:parentID/:name          TD.CreateFile
GET     /td/ListDirectory/:directoryID          TD.ListDirectory
POST    /td/Delete/:nodeID                      TD.Delete

# Ignore favicon requests
GET     /favicon.ico                            404

# Map static resources from the /app/public folder to the /public path
GET     /public/*filepath                       Static.Serve("public")

# Catch all, this will route any request into the controller path
#
#                    **** WARNING ****
# Enabling this exposes any controller and function to the web.
# ** This is a serious security issue if used online **
#
# For rapid development uncomment the following to add new controller.action endpoints
# without having to add them to the routes table.
# *       /:controller/:action                    :controller.:action
